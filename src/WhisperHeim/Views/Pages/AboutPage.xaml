<UserControl x:Class="WhisperHeim.Views.Pages.AboutPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" HorizontalAlignment="Center" MaxWidth="500">
            <!-- App info -->
            <ui:SymbolIcon Symbol="Mic24" FontSize="48" Margin="0,0,0,16"
                           HorizontalAlignment="Center" />
            <TextBlock Text="WhisperHeim"
                       FontSize="24"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Live dictation powered by Whisper"
                       FontSize="14"
                       Opacity="0.6"
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0" />
            <TextBlock Text="Version 0.1.0"
                       FontSize="12"
                       Opacity="0.4"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0" />

            <!-- Separator -->
            <Separator Margin="0,24,0,16" />

            <!-- Model status section -->
            <TextBlock Text="AI Models"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,12" />

            <ItemsControl x:Name="ModelList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="12"
                                Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0"
                                           Text="{Binding Name}"
                                           FontWeight="SemiBold"
                                           FontSize="13" />

                                <TextBlock Grid.Row="0" Grid.Column="1"
                                           Text="{Binding StatusText}"
                                           FontSize="12"
                                           Foreground="{Binding StatusBrush}"
                                           VerticalAlignment="Center" />

                                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                           Text="{Binding Description}"
                                           FontSize="11"
                                           Opacity="0.6"
                                           Margin="0,4,0,0" />

                                <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                           Text="{Binding SizeText}"
                                           FontSize="11"
                                           Opacity="0.4"
                                           Margin="0,2,0,0" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
